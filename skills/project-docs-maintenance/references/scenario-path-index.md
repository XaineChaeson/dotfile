# Scenario Path Index

Map a task to the exact entrypoint, required config, and example. Use the table for quick navigation, then provide a short scenario card for each row. Do not repeat full parameter lists; link to API overview and interface docs instead.

## Quick Map
| Scenario | Priority | Entrypoint | Required config | Example | Notes |
|----------|----------|------------|-----------------|---------|-------|
|          |          |            |                 |         |       |

---

## Scenario: <task name>
**Goal**: <what the user wants to accomplish>
**Entrypoint**: `<cli command or module.Class.method>`
**Required config**: `<non-default required flags or settings>`
**Minimal example**:
```bash
<short runnable example>
```
**Common pitfalls**: <constraints, edge cases, or error triggers>
**Related docs**: <api overview or interface reference links>

## Scenario: <task name>
**Goal**: <what the user wants to accomplish>
**Entrypoint**: `<cli command or module.Class.method>`
**Required config**: `<non-default required flags or settings>`
**Minimal example**:
```bash
<short runnable example>
```
**Common pitfalls**: <constraints, edge cases, or error triggers>
**Related docs**: <api overview or interface reference links>
